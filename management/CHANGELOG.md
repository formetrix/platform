# Changelog

## About This Document

### Purpose

This document is the version-by-version changelog for the Formetrix platform repository. It records what was actually built, in the order it happened, so that anyone reviewing the project — founder, future team member, or AI engineering agent — can trace what exists in the codebase back to when and why it arrived, without relying on memory or on git history alone.

### How to Maintain This Document

- Add a new entry the moment a notable, verified change lands — in the working tree or in a commit — not retroactively and not in advance of the work actually being done.
- Either the founder or an AI engineering agent may add an entry, but it must describe only work that has actually happened, consistent with the FORMETRIX.md rule against fabricating status or functionality.
- Group entries under a version heading using the standard Added / Changed / Fixed categories; omit any category with nothing to report for that version.
- If the state of the codebase differs from the state of git history — for example, verified work exists in the working tree but has not yet been committed — say so explicitly in the version's entry rather than implying the two are in sync.
- New version sections are added above older ones, so the most recent version is always at the top, directly under this section.
- Reference the related ticket (FM-XXXX) or decision (ADR-XXXX) ID in parentheses at the end of an entry whenever the change traces to one.
- Use the exact field order and heading levels shown in the Structure section below for every entry.

### Structure

Each version entry uses the following fields, in this order:

- **Version header** — `## [version] - date or date range`, using the version number and the date(s) the work in that version covers.
- **Note** (optional) — a plain-sentence callout used when the state of git history needs to be stated explicitly, such as work being verified but not yet committed.
- **Category groups** — one or more of `### Added`, `### Changed`, `### Fixed`, each followed by a bullet list; omit any category with no entries for that version.
- **Entry** — one bullet line per change, with a related ticket/decision ID in parentheses where applicable.

**Template**

```markdown
## [X.Y.Z] - [YYYY-MM-DD or date range]

**Note:** [State plainly if git history and working-tree state diverge, or omit this line]

### Added

- [Describe the change in one line.] ([FM-XXXX] or [ADR-XXXX] if applicable)

### Changed

- [Describe the change in one line.] ([FM-XXXX] if applicable)

### Fixed

- [Describe the change in one line.] ([FM-XXXX] if applicable)
```

---

## [0.1.0] - 2026-07-29 to 2026-07-30 (Unreleased)

**Note:** The foundation and management-system work below was committed locally as `7e0db87` ("FM-0001: Initialize Formetrix foundation"), but that commit has not been pushed to GitHub yet — local `main` is ahead of `origin/main`. The Supabase application foundation entry below (FM-0021) was built on top of that commit and, as of this note, is itself still uncommitted in the working tree. See FM-0003.

### Added

- Repository created on GitHub (Formetrix/platform).
- Founder Pack established as the source constitution and initial product, architecture, database, and UI reference docs. It is maintained alongside this repository for now; importing it into platform/docs is tracked as FM-0002.
- FORMETRIX.md adopted into the platform repository as the binding project constitution (ADR-0009).
- Next.js 15 application foundation scaffolded and verified: App Router structure, TypeScript, Tailwind CSS v4, ESLint/Prettier, Supabase client placeholders, Mapbox integration placeholder, dark/light theming, error boundaries, loading states, and a basic auth structure placeholder.
- Formetrix Project Management System created (management/): MILESTONES.md, TICKETS.md, DECISIONS.md, MEETINGS.md, CHANGELOG.md (FM-0001).
- Configured Supabase application foundation: browser client, server client, a dormant session-refresh middleware utility, centralized environment-variable validation, and a manually-invoked health-check utility, all under `src/lib/supabase/`. No live Supabase project is connected and no authentication, tables, or Row Level Security were added (FM-0021; see TICKETS.md's note on that ticket's ID and ADR-0010).
- Defined the initial conceptual domain model (`docs/DOMAIN_MODEL.md`): Organization, User, Membership, Property, Parcel, Scenario, Assumption, Analysis, Constraint, Result, Recommendation, Report, and Data Source, with explicit Version 1 boundary recommendations, a conceptual relationship diagram, and blocking/non-blocking open questions for founder review. No schema, migrations, or application code were added (FM-0022; ADR-0011 through ADR-0014).
- Founder domain decisions finalized (FD-0001 through FD-0009): resolved all five previously-blocking Open Questions plus two non-blocking ones, established Property as a long-lived workspace carrying a lifecycle status (Discovered → Archived in Version 1; Planning → Completed explicitly deferred), and finalized the Version 1 domain boundary with no concept left as "Requires founder decision." `docs/DOMAIN_MODEL.md` updated accordingly. No schema, migrations, or application code were added (FM-0023).
- Founder decision log created (`management/FOUNDER_DECISIONS.md`) — the permanent record of product decisions, distinct from the architecture-focused `management/DECISIONS.md` (FM-0023; ADR-0015).
- Founder Pack governing docs (`docs/PRODUCT.md`, `ROADMAP.md`, `ARCHITECTURE.md`, `DATABASE.md`, `UI.md`) and `.cursor/rules/formetrix.mdc` copied into this repository, content verified to match the source exactly (FM-0002).
- Established `platform/` as the sole canonical Formetrix repository; no external directory, including the former Founder Pack reference folder, is an active project reference going forward (ADR-0016).
- Designed the Project Control Dashboard architecture (`docs/PROJECT_DASHBOARD_ARCHITECTURE.md`): six `management/data/*.json` schemas, nine dashboard views, progress-calculation formulas (computed, not hand-set), an update/validation/audit workflow, and an automation design. Found that the computed formula puts Milestone 0 at ~60%, notably below the hand-set 80% — flagged as the design's central finding, not silently reconciled. No dashboard, JSON data, or hooks were built (FM-0024; ADR-0017 through ADR-0019).
- The foundation and management-system work (everything through FM-0023) was committed and pushed to `origin/main` as `92d0b03` ("FM-0001: Establish Formetrix engineering foundation") — FM-0002 and FM-0003 are now Completed, not just In Progress.
- Built the Project Control Dashboard MVP at `/internal/project-dashboard`, populated from real `management/data/*.json` (not examples): executive summary, milestone progress, ticket kanban board, roadmap, activity feed, decision log, risks/blockers, and release history, all typed and validated before rendering. Progress is computed live from ticket data — Milestone 0 now shows 73% (computed), not the previously hand-set percentage. No Excel, PDF, authentication, hooks, or GitHub Issues integration were added (FM-0025).
- Applied the Founder-approved Formetrix brand system to the dashboard (Deep Navy/Charcoal/Electric Cyan/Light Gray/White; Inter + Space Grotesk via the existing `next/font/google` mechanism) and built a reusable interactive detail-drawer system — every ticket, milestone, roadmap entry, decision, activity item, release, and summary card now opens a keyboard-accessible detail panel sourced entirely from `management/data/*.json`. Light-mode cyan is deliberately darkened (`#0E7490`) for WCAG contrast, documented as ADR-0020 rather than silently deviating from the brand hex. `docs/DESIGN_SYSTEM.md` created as the authoritative visual spec; `docs/UI.md` now references it. Milestone 0 recomputes to 75% with FM-0026 added. No drag-and-drop, authentication, Excel, PDF, hooks, or GitHub Issues integration were added (FM-0026; ADR-0020, ADR-0021).
- Fixed a reported `useDashboardDetail must be used within a DashboardDetailProvider` runtime error by collapsing the dashboard's nine independent server→client boundaries into one client shell (`ProjectDashboardShell`) — `page.tsx` now only loads and validates data. The original tree was structurally correct and server-rendered cleanly (confirmed via `curl`), so the fix removes a class of Fast-Refresh fragility rather than patching a broken wrapper; no fallback context value or hook change was added. Verified via a clean production build (`next start`) and three consecutive fresh `next dev` requests. Milestone 0 recomputes to 77% with FM-0026A added (FM-0026A; ADR-0022).
- Added a prominent Current Work section to the top of `/internal/project-dashboard` (after the header, before the executive summary) and a Project Codes / Naming Key legend. Current Work shows the active milestone, active engineering ticket, status, priority, computed progress, workstream, description, dependencies, blocker (when present), last-updated time, and the next action; the card opens the full ticket in the existing detail drawer. The active milestone (milestone progress + roadmap) and active ticket (Kanban) are highlighted with a restrained cyan treatment and a "Current" badge. The legend explains M, FM, ADR, FD, v, PR, and commit SHA — with numbering and storage location — from a typed config (`src/features/project-dashboard/config/project-codes.ts`). Current work is resolved from typed structured data (`management/data/project-status.json`, extended with `currentTicketId`/`currentWorkstream`/`currentFocusSummary`/`currentNextAction`) and validated: inconsistent pointers surface a visible warning instead of a guessed value. Milestone 0 recomputes to 78% with FM-0027 (In Progress) added (FM-0027).
- Added a deterministic, local Dashboard Intelligence system: `scripts/update-dashboard-intelligence.ts` (`npm run dashboard:update` / `dashboard:check`) recomputes every calculated Mission Control field — progress, ticket/status counts, current work, integrity warnings, and `dataIntegrity` health — from `management/data/*.json`, writing Prettier-formatted output only when something changed (idempotent). `scripts/record-validation-health.ts` (`npm run dashboard:health`) records real lint/typecheck/format/build results into a new `health` block that distinguishes `passing`/`failing` from `unknown`/`not_configured` and never claims unverified integrations are healthy. Current-ticket selection is driven by explicit status (in_progress/review/blocked), never by guessing a "next" ticket; multiple-active without a selected primary raises a visible warning. Milestone/overall progress switched to completed ÷ total scoped tickets (equal-weighted overall), superseding the criteria-mean (ADR-0024). Added a Mission Control status panel (last update, active count, integrity, current blocker, health checks), `docs/MISSION_CONTROL.md`, and a Definition of Done in `.cursor/rules/formetrix.mdc` requiring the intelligence + health runs before any ticket is marked Completed. Added `tsx` as a dev-only dependency to run the scripts (ADR-0023, ADR-0025). FM-0027 closed out; Milestone 0 recomputes to 80% with FM-0028 completed (FM-0028).
- Built the Property Workspace Foundation, the first investor-facing product feature: `/properties` (list) and `/property/[id]` (workspace), backed entirely by typed mock data — no Supabase, calculations, reports, PDF, or AI. The workspace shows persistent chrome (name, address, parcel/APN, city, state, acres, zoning, status, map placeholder, summary), a left navigation for all 9 future sections (only Overview implemented; the other 8 render a shared `ComingSoonPanel` via real routes), and an Overview panel of 5 cards — Property Facts, Parcel Facts, Development Snapshot, Current Recommendation, Unknowns — every value sourced from `src/features/properties/data/mock-properties.ts` and grounded in `docs/DOMAIN_MODEL.md`'s Property/Parcel/Constraint/Recommendation/Unknown model. `Badge` and `interactiveCardClass` were promoted from the dashboard feature to shared UI/`lib/utils` now that a second feature genuinely needs them (FORMETRIX.md §24). `/property/[id]` uses `generateStaticParams` + `dynamicParams = false` so an unknown property id returns a real HTTP 404 rather than 200 — the pre-existing root `loading.tsx`'s streaming boundary otherwise locks the response status at 200 even when `notFound()` fires deeper in the tree, a documented Next.js App Router limitation (ADR-0026). Milestone 2 gains its first completed ticket with FM-0029 (FM-0029; ADR-0026).
- Designed the initial core database schema (`docs/DATABASE_SCHEMA.md`), architecture-only — no SQL, Prisma, Supabase migrations, or live tables. Defines five core entities (User, Organization, OrganizationMember, Property, PropertyWorkspace) with fields, logical data types, required/optional, primary/foreign keys, unique constraints, relationships, cardinality, ownership/tenancy rules, future extension points, and a Mermaid ER diagram, all consistent with `docs/DOMAIN_MODEL.md` and FD-0002–FD-0005. Introduces **PropertyWorkspace** as the 1:1 evaluation surface future analysis modules attach to (Parcel, Zoning, Constraints, Assumptions, Financial, Recommendation, Documents, Activity — extension points only), a refinement of the domain model recorded as ADR-0027; models User↔Organization as a many-to-many `OrganizationMember` join with V1's one-org-per-user enforced at the application layer, and keeps parcel facts (APN/acreage/zoning) on Parcel rather than duplicated onto Property (ADR-0028). `docs/DOMAIN_MODEL.md` §4/§5.4a updated to add PropertyWorkspace to the canonical vocabulary (FM-0007; ADR-0027, ADR-0028).
- Designed authentication and organization architecture (`docs/AUTH_FLOW.md`), planning only — no Supabase Auth configuration, OAuth, migrations, or React components. Covers sign-in/up, password reset, email verification, organization creation/invitation/switching, account settings, UI screen list with validation/error/empty states and future MFA, security considerations, and provider compatibility (Google/Microsoft, magic links, enterprise SSO). Defines membership roles Owner/Admin/Member/Viewer (ADR-0029) and active-organization context (ADR-0030); aligns `OrganizationMember.role` in `docs/DATABASE_SCHEMA.md` to include `viewer`. Multi-org switching is designed for schema capacity while FD-0002's V1 one-org policy remains an application constraint (FM-0008; ADR-0029, ADR-0030).
- Implemented Supabase session-refresh middleware and protected routes (FM-0009): root `src/middleware.ts` calls existing `updateSession` (`getClaims` + cookie write-through), centralized route policy in `src/lib/auth/routes.ts`, open-redirect-safe `next` return paths, and `getAuthenticatedUser()` via verified `getUser()`. Unauthenticated/unconfigured access to `/properties`, `/property/*`, `/settings`, `/organization/*` redirects to minimal `/auth/sign-in` / `/auth/sign-up` placeholders (no forms). `/internal/project-dashboard` stays public for local Mission Control (ADR-0031). Public home and static assets remain accessible; missing Supabase env never simulates a signed-in user. Focused Node tests cover route/return-path helpers (`npm run test`).
- Implemented organization and workspace membership foundation (FM-0010): migration-ready SQL for `user_profiles`, `organizations`, and `organization_memberships` with RLS (`SECURITY DEFINER` helpers, self-role-change trigger), typed server helpers in `src/lib/organizations/`, V1 one-active-membership index, and active-org selection that never trusts unverified client org ids (ADR-0032). Statuses `invited|active|suspended|removed`; roles Owner/Admin/Member/Viewer. No onboarding/invitation UI; migrations are not auto-applied; `supabase/seed.dev.example.sql` is development-only and commented.
- Implemented Property and Parcel PostGIS schema (FM-0011): migrations enable PostGIS and define org-private `properties`, shared `parcels` (MultiPolygon/Point EPSG:4326, provider provenance, GIST indexes), and `property_parcels` (m:n, one primary per property) with RLS reusing org helpers; typed helpers in `src/lib/properties/` for CRUD/attach/list and centralized status transitions; no Property Workspace UI, Regrid, or Mapbox (ADR-0033).
- Implemented Regrid parcel search and property creation services (FM-0012): typed server-only Regrid client (`src/lib/regrid/`) with env validation, retry/rate-limit handling; ingestion services `searchParcels` / `importParcel` / `createPropertyFromParcel` / `refreshParcel` with typed Results; parcel upsert by `(provider, provider_parcel_id)` via service-role RPC; provenance preserved in `raw_source_metadata` + timestamps; unit tests with mocked fetch (no live token required); no UI, map, or workspace (ADR-0034).
- Built the Version 1 Property Workspace on live services (FM-0013): `/properties` and `/property/[id]` read Organization Properties and linked Parcels (mock data removed); Overview shows summary, parcel card with expandable provenance, timeline, and recommendation placeholder; section nav + lazy-load-ready Coming Soon modules; `PropertyStatusBadge` uses centralized status labels; empty states when Supabase/Regrid/data unavailable; responsive Formetrix design (ADR-0035).
- Built the Property Dashboard as the central `/property/[id]` landing (FM-0014): identity (what) and location (where), data-availability indicators, available datasets/analyses inventory with honest `not_built` states, parcel/property summaries, timeline, recommendation placeholder, and module quick-nav — derived from live services only; no Mapbox, zoning, or financial engines (ADR-0036).
- Provisioned and connected the hosted Supabase project (FM-0005): prefer `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` with centralized legacy anon fallback (ADR-0037); branded `supabase_unconfigured` auth screen; README/AUTH_FLOW/ARCHITECTURE Founder checklist and CLI docs; `supabase init` config.toml. Linked the hosted project (`pdzokayrbvihjavkrcze`) via the Supabase CLI, verified connection health (GoTrue `/auth/v1/health` 200 via publishable key), reviewed migrations, showed a `db push --dry-run`, and applied all 6 migrations with Founder approval (`migration list` confirms local == remote). Post-push schema verification confirmed org/property/parcel tables with RLS, PostGIS 3.3.7, and `upsert_parcel_from_provider`. Service-role key stays server-only; no seeds/dev users applied; no secrets committed. Mission Control `health.supabase` is `passing` (ADR-0037; FM-0005).
- Connected Vercel deployment for the platform (FM-0006): GitHub `formetrix/platform` linked to Vercel project `formetrix/platform` (production branch `main`). Fixed Framework Preset Other→Next.js (resolved production `NOT_FOUND`). Production URL https://platform-pi-olive-13.vercel.app. Hosted env vars configured (URL + publishable key + SITE_URL). After redeploy, home and Mission Control return 200; `/properties` redirects to unauthenticated sign-in placeholder (not `supabase_unconfigured`). Supabase Auth Site/Redirect URLs documented (Founder-configured for production + localhost). No custom domain; no secrets committed. Mission Control `health.deployment` is `passing`.
- Integrated Mapbox parcel visualization on the Property Dashboard (FM-0015): installed `mapbox-gl`; live PostGIS boundaries via `parcel_geometries_geojson` (`ST_AsGeoJSON`); fit bounds, property/centroid marker, street/satellite styles, precision caption, empty states when token or geometry is missing; never mock outlines (ADR-0038).
- Implemented zoning data model and Zoning Overview (FM-0016): normalized municipality/district/overlays/land uses/dimensional regulations linked per parcel with multi-provider provenance; `ZoningOverview` on Property Dashboard and `/property/[id]/zoning`; honest missing states; service-role upsert RPC for future providers (ADR-0039).
- Implemented the production authentication UI and first-login organization setup (FM-0006A), replacing the `/auth/sign-in` and `/auth/sign-up` placeholders with working Supabase flows: sign-in (show/hide password, sanitized `next` return path), sign-up (full name, email, password + confirmation, terms, email-verification state), forgot password, reset password from a verified recovery session, sign-out, and a `/auth/confirm` route handler that accepts both emailed link shapes (`token_hash` → `verifyOtp`, PKCE `code` → `exchangeCodeForSession`) and lands failures on sign-in with a readable reason. Every flow is a Server Action posting from a plain form, so the password never enters client JavaScript and `@supabase/ssr` writes the session cookies in the same round-trip (ADR-0040) — which also means the forms work without JavaScript. After authentication the server resolves the profile and membership: no usable organization routes to `/onboarding/organization`, otherwise to the sanitized `next` (default `/properties`); a _failed_ lookup is treated as "carry on", never as "no organization", so nobody with an existing organization is nudged into creating a duplicate. Organization setup creates the Organization, the owner Membership, and the `active_organization_id` preference in a single `SECURITY DEFINER` transaction (`create_organization_with_owner`), and `user_profiles` rows are now provisioned by an `on_auth_user_created` trigger with an application-side repair path (ADR-0041; migration `20260804070000`). Error messages are enumeration-safe by construction — sign-in never distinguishes an unknown email from a wrong password, and forgot-password shows the same confirmation either way. Route policy gained `/auth/forgot-password`, `/auth/reset-password`, `/auth/confirm`, and protected `/onboarding/*`; return-path validation now rejects every `/auth/*` path, while middleware deliberately does _not_ bounce authenticated visitors off reset-password or confirm (a recovery link is what creates that session). Invitations, organization switching, OAuth, and MFA remain out of scope.
- Fixed public environment variables being invisible to the browser (ADR-0042): `NEXT_PUBLIC_*` values were read through a computed `process.env[name]` lookup, which Next.js does not inline into client bundles, so every Client Component saw them as unset. Reads are now static member accesses. Confirmed against the built chunks — the Supabase URL, publishable key, and Mapbox token are inlined and no `process.env` reference survives. This had also been silently disabling the FM-0015 parcel map, whose `isMapboxConfigured()` check runs client-side; it went unnoticed because that ticket could only verify server-rendered markup (FM-0006A; FM-0015).
- Made the header auth-aware: `AuthProvider` now subscribes to `supabase.auth.onAuthStateChange` and the header shows the signed-in email with a sign-out control, replacing the always-signed-out placeholder. This is presentation only and is documented as such — route access remains enforced by middleware and by each page's verified `getAuthenticatedUser()` call (FM-0006A).
- Restricted the parcel and zoning ingestion RPCs to the Supabase service role (FM-0030). `upsert_parcel_from_provider` and `upsert_parcel_zoning_from_provider` are `SECURITY DEFINER` and bypass RLS — they are the only write path into `public.parcels`, which grants `authenticated` no INSERT/UPDATE policy at all. Both original migrations ended with `revoke all ... from public`, which reads as "service role only" but is not: Supabase's default privileges grant EXECUTE on new functions in `public` directly to the named `anon`/`authenticated` roles, and `REVOKE ... FROM PUBLIC` does not remove a grant held by a named role. A configuration audit confirmed `has_function_privilege('anon', ...)` was true on the hosted project, meaning any holder of the publishable key could insert or overwrite shared parcel and zoning reference data. Migration `20260804180000` now revokes from `public`, `anon`, and `authenticated` and re-grants only to `service_role`, looping over `pg_proc` by name so future overloads are covered too, and self-verifies with `has_function_privilege` — raising rather than reporting success if any untrusted role retains access. Verified against the hosted project: anonymous and real signed-in callers both receive SQLSTATE 42501 permission denied on both functions, while service-role ingestion still imports a live Regrid parcel with geometry and reuses the existing row on a repeat import. No application code or UI changed (ADR-0043).
- Added `src/lib/properties/ingestion/rpc-permissions.test.ts`, which fails the build if any migration re-grants an ingestion RPC to `anon` or `authenticated`, if the revokes go missing, or if the parcel store stops using the service-role client. Confirmed to actually fail by temporarily reintroducing the original mistake. An opt-in live probe (`FORMETRIX_LIVE_RPC_CHECK=1`) asserts SQLSTATE 42501 from a publishable-key caller against the configured project; it is skipped by default so `npm run test` stays offline and deterministic (FM-0030).
- Fixed live Regrid search normalization (FM-0030B). Regrid's v2 API returns candidate groups keyed by record type — `parcels`, `buildings`, `zoning` — and nests every parcel attribute one level down under `properties.fields`. The parser written in FM-0012 read `collection.features` and `properties.<attr>`, so live address search returned **zero candidates**, and the rare candidate that did resolve (via `ll_uuid`, which happens to also sit at the top level) lost its APN, address, city, state, county, ZIP, and acreage. `extractParcelFeatures` now reads the `parcels` group when the grouped shape is present and treats it as authoritative — an absent or empty group means zero parcels, never "look in `buildings`", since a building footprint is not a land record. The legacy bare `FeatureCollection` remains supported and its original test passes unchanged. Field mapping picks up the live names (`state2` for state, `scity` for the situs city — live `city` is a URL slug, `szip`/`szip5`, string `lat`/`lon`), and `latitude`/`longitude` were added to the normalized candidate so provider coordinates survive. Malformed payloads return no candidates rather than fabricated ones. Verified against the real API: valid address 2 candidates with every field populated, invalid address 0, APN 1, point 3, lookup by provider id resolved, malformed JSON raises `parse_error`, rate limiting still retries and preserves `Retry-After`.
- Fixed two Regrid request defects found during that live verification (FM-0030B). APN search sent `apn=`, which the v2 API rejects with HTTP 400 "Please provide a 'parcelnumb' parameter"; it now sends `parcelnumb`. Parcel lookup requested `{baseUrl}/parcels/{id}`, an unversioned path that serves HTML and 404s; it now uses `/api/v2/parcels/{id}`. Both are covered by new client tests. All three defects share one root cause: the FM-0012 client was written against an assumed contract and never exercised against the live API, and its unit tests mocked that assumption — so they passed while every real call failed.
- Built the complete Add Property workflow (FM-0031) — the first end-to-end property onboarding path, and the first caller the Regrid ingestion services have ever had. An organization with no properties now sees "No properties yet / Import your first parcel to begin evaluating development opportunities" with **Add Property** as the primary action. The button opens a shared responsive `Modal` (centered dialog from the `sm` breakpoint up, full-screen sheet on phones; focus trapped, Escape to close, focus restored, body scroll locked) that searches live Regrid by street address or APN, lists candidates with address, APN, city/county/state, parcel size, provider, and match score, previews the selected parcel with its boundary rendered through the same `ParcelMap` the workspace uses, imports it, and routes to the new Property Workspace. Business logic lives in `add-property-workflow.ts`, which imports no React and no Next — the Server Actions are thin wrappers over it, so the planned iPad client can drive the same workflow through a Route Handler without a second implementation of search, normalization, import, or property creation. Duplicate imports are caught before any write by a new `findOrganizationPropertyByParcel` lookup and offer to open the existing Property; because parcel rows are shared across organizations, only an existing _Property in your own organization_ counts as a duplicate. Every failure mode — no results, provider unavailable, rate limit, duplicate, network error, unauthorized, missing organization, unconfigured import — has its own message, and unit tests assert they stay distinct, always say nothing was saved, and never leak a provider string or environment variable name.
- The Add Property client sends only the provider parcel id, never the candidate object, so the server re-fetches authoritative provider data at import time (FM-0031). Besides removing any question of what a tampered candidate could write, this was forced by a real failure: embedding the raw provider feature in a Server Action argument produced `Connection closed.` (HTTP 500) during argument deserialization, isolated by bisecting the payload.
- Fixed a silent data-loss path in `createPropertyFromParcel` (FM-0031): importing by provider id reconstructed the candidate from the stored parcel row with `city: null` and `postalCode: null` hard-coded, so those Properties would have been created with no city or ZIP. `importParcel` now returns the normalized candidate it used and the creation path prefers it. Verified: an imported Property carries city, ZIP, and coordinates.
- Promoted `useFocusTrap` from the project-dashboard feature to `src/components/ui/` now that the properties feature needs it too (FORMETRIX.md §24), matching the precedent set by `Badge` in FM-0029.
