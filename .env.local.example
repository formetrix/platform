# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored — never commit real credentials.
#
# Nothing in this project reads these yet (Supabase and Mapbox are not
# connected — see FORMETRIX.md and the foundation README). This file
# exists so the required shape is documented in one place.

# --- Supabase --------------------------------------------------------
# Project Settings > API in the Supabase dashboard.
# The anon key is safe to expose to the browser; it is constrained by
# Row Level Security policies, not secrecy.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Bypasses Row Level Security — never prefix with
# NEXT_PUBLIC_, never send to the client, never log.
SUPABASE_SERVICE_ROLE_KEY=

# --- Mapbox ------------------------------------------------------------
# https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=

# --- Site ----------------------------------------------------------------
# Public base URL of this deployment, used for absolute links/metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
