# FM-0013 Property Workspace visuals

Design-faithful mockups of the Version 1 Property Workspace (dark mode,
Electric Cyan accents, Inter, soft elevation). Live UI reads real Property
services — these images illustrate layout for investor review when a signed-in
session with data is not available in CI.

| File                         | View                                     |
| ---------------------------- | ---------------------------------------- |
| `workspace-desktop.png`      | Desktop Overview with parcel + timeline  |
| `workspace-empty-mobile.png` | Mobile empty parcel / Regrid unavailable |
